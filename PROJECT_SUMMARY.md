# Project Summary: @ossrandom/design-system

> Generated by `project-summarizer` on 2026-04-26. Audience: AI agents (and humans) who need to understand and modify this codebase. Every claim is checkable against a file or command; items marked `[inferred]` were not directly verified.

## Identity

- **What it is:** A strongly-typed React component library — the **RandomCodeSpace (RCS) Design System**. Ships ~50 components, design tokens, and a single bundled stylesheet. Brand: Signal Red (`#E60000`) on Cod Gray (`#1C1C1C`); Inter for UI, JetBrains Mono for code.
- **Type:** **Hybrid** — (a) an npm component library published as `@ossrandom/design-system`, AND (b) a Claude Code user-invocable skill (`SKILL.md` at root, `name: randomcodespace-design`). The skill exposes the same tokens / kits to Claude for visual prototyping; the npm package ships the React runtime.
- **Status:** **Pre-release / freshly bootstrapped.** Version `0.1.0` (never released — package.json:3, no git tags). Four commits in the entire history, all on 2026-04-26 (`git log` — three are `checkpoint: pre-yolo`, one `Initial Commit`). Branch `main` is **3 commits ahead of `origin/main`** and has not been pushed; only `b5f630a Initial Commit` exists upstream.
- **Primary language(s):** TypeScript 5.6+ (`.tsx` runtime, `.d.ts` type-only), React 18+ peer. JSX (untyped) is used **only** in `ui_kits/` for static prototype kits — not part of the published package.

## Tech stack

- **Frameworks / key libs:** React + ReactDOM ≥18 (peer only — `package.json:44-46`). **Zero runtime dependencies.**
- **Datastore(s):** None.
- **Build system:** `tsc -p tsconfig.build.json` + an inline Node script that concatenates `colors_and_type.css` + `src/styles.css` into `dist/styles.css` (`package.json:37`).
- **Test framework:** `vitest` declared (`package.json:55`) — **no test files exist yet** (`find ... -name '*.test.*' -o -name '*.spec.*'` returns empty). CI's test step is `continue-on-error: true` (`.github/workflows/ci.yml:38-39`).
- **Lint:** `eslint` + `@typescript-eslint/*` declared (`package.json:50-53`) — **no eslint config file present** (no `.eslintrc*`, no `eslint.config.*` at root). CI's lint step is `continue-on-error: true` (`.github/workflows/ci.yml:34-35`).
- **Package manager:** `pnpm@9.12.0` (`package.json:58`). Engines: `node >= 18.18` (`package.json:57`).
- **Manifest files read:** `package.json`, `tsconfig.json`, `tsconfig.build.json`, `pnpm-lock.yaml` (existence only — not read), `.gitignore`, `.npmignore`, `.github/workflows/ci.yml`, `.github/workflows/release.yml`.

## Entry points

| Entrypoint | File | Purpose |
|------------|------|---------|
| Public ESM entry | `src/index.tsx` (34 lines) | Re-exports every runtime component + `export type * from "./tokens"` and `"./components"`. |
| Token types | `src/tokens.ts` (56 lines) | Strongly-typed token unions: `BrandColor`, `Size`, `SpaceSize`, `Radius`, `ThemeMode`, `Direction`, `Axis`, `Align`, `Justify`, `TypeScale`, … |
| Component prop types | `src/components.d.ts` (648 lines, **`.d.ts` not `.ts`**) | Single types-only file with `BaseProps` and every `*Props` interface. Hand-written; tsc copies it through. |
| Internal utilities | `src/internal/cx.ts` (21 lines) | `cx(...parts)` className composer, `noop`, `uid(prefix?)` counter. Shipped to consumers — keep tiny. |
| Component runtime | `src/components/*.tsx` (13 files, ~2.5k LOC total) | One file per component category. |
| Runtime stylesheet (source) | `src/styles.css` (916 lines, 251 `.rcs-*` classes) | Component styles, sectioned by category. |
| Token stylesheet (source) | `colors_and_type.css` (root, 330 lines) | CSS variables for color/type/spacing/radii/shadows/motion + light + dark themes + `prefers-color-scheme` fallback. |
| CSS subpath | exports `./styles.css` → `dist/styles.css` (`package.json:31`) | Concatenation of the two CSS files above. |
| Skill manifest | `SKILL.md` (frontmatter `name: randomcodespace-design`, `user-invocable: true`) | Tells Claude how to invoke the brand for design tasks. **Not** a project README — it is content the skill ships. |

## Directory map

```
design-system/
├── src/                          — published source
│   ├── index.tsx                 public ESM entry; re-exports everything
│   ├── tokens.ts                 token type unions
│   ├── components.d.ts           every prop interface (types-only, hand-written)
│   ├── styles.css                .rcs-* component styles
│   ├── internal/
│   │   └── cx.ts                 cx / noop / uid
│   └── components/               13 .tsx files, one per category
│       ├── buttons.tsx           Button, IconButton, ButtonGroup
│       ├── inputs.tsx            Input, NumberInput, PinInput, Textarea
│       ├── selects.tsx           Select<V>, Combobox<V>
│       ├── form-controls.tsx     Checkbox, RadioGroup<V>, Switch, Slider, DatePicker, DateRangePicker, FileUpload, FormField
│       ├── badges.tsx            Badge, StatusDot
│       ├── layout.tsx            Card, Space, ScrollDiv, Divider, Grid (+ Grid.Col)
│       ├── navigation.tsx        Tabs<K>, Menu<K>, Breadcrumb, Pagination, Steps
│       ├── feedback.tsx          Alert, Modal, Drawer, Progress, Skeleton, Spin, Tooltip, toast, ToastRegion
│       ├── data-display.tsx      Table<T>, Stat, Avatar, Timeline
│       ├── chat.tsx              Chat
│       ├── code.tsx              CodeBlock, Markdown, Terminal, RichTextEditor
│       ├── page.tsx              PageHeader, AppShell
│       └── theme.tsx             ThemeProvider, useTheme
├── colors_and_type.css           — design-token stylesheet (lives at repo root, not in src/)
├── preview/                      — 39 standalone HTML "design gallery" cards (one concept per file) + _card.css
├── ui_kits/                      — 14 .jsx static reference layouts (NOT shipped to npm; not strict TS)
│   ├── marketing/                Hero, Nav, FeatureGrid, CLIShowcase, Pricing, Footer, index.html
│   ├── app/                      AppShell-style: Sidebar, Topbar, ServicesGrid, LogTail, CommandMenu, index.html
│   └── docs/                     DocsSidebar, Article, TOC, index.html
├── assets/                       — brand SVG/PNG marks + self-hosted fonts (Inter, JetBrains Mono in 400/500/600)
├── screenshots/                  — marketing-after.png, marketing-check.png (visual checks; not shipped)
├── .github/
│   ├── workflows/ci.yml          typecheck → lint(continue) → test(continue) → build → upload dist
│   ├── workflows/release.yml     tag-driven dual-publish (npm + GitHub Packages) + GitHub Release
│   ├── PUSH.md                   options for first push to GitHub
│   ├── SETUP.md                  one-time setup checklist (NPM_TOKEN, environments, first release)
│   └── RELEASING.md              version-bump cheatsheet
├── package.json
├── pnpm-lock.yaml
├── tsconfig.json                 strict; ESNext modules; rootDir src/; outDir dist/
├── tsconfig.build.json           extends above; emits .js + .d.ts; excludes tests + src/styles.css
├── README.md                     consumer-facing
├── SKILL.md                      Claude skill manifest (content, not project metadata)
├── LICENSE                       MIT
├── .gitignore
└── .npmignore                    excludes .github/, preview/, scrap/, tests, eslint/prettier/editor configs
```

Generated artifacts (`dist/`, `node_modules/`, `*.tsbuildinfo`) are git-ignored.

## Run, build, test

Commands taken from `package.json:35-43` and `.github/workflows/ci.yml`:

```bash
# install
pnpm install

# core dev loop
pnpm typecheck       # tsc --noEmit
pnpm lint            # eslint "src/**/*.{ts,tsx}"   — currently no eslint config; will fail until one lands
pnpm test            # vitest run                    — currently no tests; effectively a no-op
pnpm build           # tsc -p tsconfig.build.json + inline node script that concatenates the two CSS files

# preview (manual)
pnpm preview         # echoes "Open preview/responsive-check.html in a browser" — not a server
```

The build emits `dist/index.js`, `dist/index.d.ts`, declaration maps, source maps, and a single `dist/styles.css` that is `colors_and_type.css` + `src/styles.css` concatenated. `prepublishOnly` runs `typecheck && build` before any publish.

**Required env / external services:** none for build/runtime. Release workflow needs `NPM_TOKEN` (repo secret) and uses the auto-provided `GITHUB_TOKEN` (`.github/workflows/release.yml:78,98`, `.github/SETUP.md`).

## Conventions an agent must respect

Top rules — see `docs/project/conventions.md` for the full set with examples.

- **One component category per file** under `src/components/`. To add a component: create or edit a category file, export the runtime, add the prop interface to `src/components.d.ts`, add a re-export line to `src/index.tsx`. Don't introduce per-component directories.
- **`src/components.d.ts` is types-only and hand-written.** Components import their props as `import type { XProps } from "../components"` (`src/components/buttons.tsx:7`). Do not generate it, do not split it, do not create a `components.ts` runtime file alongside it.
- **Every prop interface extends `BaseProps`** (`{ id, className, style, "data-testid" }` — `src/components.d.ts:25-30`) and uses `readonly` on every field.
- **Generic components** (`Select<V>`, `Combobox<V>`, `Tabs<K>`, `Menu<K>`, `RadioGroup<V>`, `Table<T>`) take their generic from a value/key/row type. Preserve generics when editing — don't widen to `any`.
- **CSS class prefix is `.rcs-*`**, BEM-ish modifiers as `.rcs-foo--variant`. `cx()` from `src/internal/cx.ts` is the only className composer; do not pull in `clsx`/`classnames`.
- **No runtime dependencies.** React/ReactDOM are `peerDependencies` (`package.json:44-46`). Adding a runtime dep is a deliberate, reviewed decision.
- **Voice/visual rules in SKILL.md (`§Non-negotiables`)** are non-negotiable for any UI work generated against this brand: monochrome palette + single Signal Red accent, 1px hairlines, 4px radius default, 140–220ms motion, Lucide icons at 1.5px stroke, no emoji, no exclamation marks, no stock imagery.

## Gotchas

Non-obvious things that bite. **Read these before making changes.**

- **`colors_and_type.css:12` `@import` from Google Fonts.** This contradicts the README/SKILL.md "self-hosted, offline-safe" claim. The local `@font-face` rules below it always win in browsers, but the `@import` itself still triggers a public-internet request. For air-gapped consumers: delete or comment out line 12 (the comment in the file even labels it a fallback for static checkers).
- **CI's `lint` and `test` steps are `continue-on-error: true`** (`.github/workflows/ci.yml:34-39`). Green CI does **not** mean lint or tests passed. The comment in the workflow says "remove once eslint config lands" / "remove once tests land" — these are tracked deferrals, not permanent.
- **No tests exist.** `pnpm test` is a successful no-op today. Don't claim "tested" without writing one.
- **No eslint config exists.** `pnpm lint` will fail at the local CLI until a config (`eslint.config.js` flat config) is added.
- **`src/components.d.ts` is `.d.ts`, not `.ts`.** TypeScript resolves `import type { … } from "../components"` to it via module resolution. Renaming/splitting it requires updating every component file.
- **Build CSS concatenation is an inline Node script in `package.json:37`.** It uses `require('fs')` even though the package is `"type": "module"` — works because `node -e` with `require` runs in CJS mode. Don't "fix" by switching to `import`; it'll break unless wrapped.
- **`tsconfig.build.json` excludes `src/styles.css`** (`tsconfig.build.json:9`) — the CSS file is intentionally inside `src/` for proximity but is **not** emitted by tsc; only the concat script copies it. Do not move it under tsc's output.
- **`ThemeProvider` writes to `document.documentElement`** (`src/components/theme.tsx:30,38,44`) — `data-theme` attribute and `--accent`/`--font-sans`/`--font-mono` inline styles. Multiple providers in one document fight each other; use one root provider.
- **`useTheme` outside a `<ThemeProvider>` returns no-op setters** with `mode` derived from `prefers-color-scheme` (`src/components/theme.tsx:60-64`). Setters silently do nothing — don't expect them to update CSS variables when used standalone.
- **The skill aspect (`SKILL.md`) and the npm package are different deliverables in one repo.** When Claude is invoked via the skill, it consumes assets directly from this directory. When users `npm install`, they get only what `package.json:files` allows: `dist/`, `src/`, `README.md` (`package.json:33`). Editing for one audience can affect the other.
- **3 unpushed commits on `main`** at session start (per `git status`). The "checkpoint: pre-yolo" commits are local checkpoints; verify intent before pushing.
- **`ui_kits/*.jsx` are loose JSX**, not part of `tsconfig.json` (`include: ["src/**/*"]`). They will not type-check; they're static reference kits for visual review (load `ui_kits/<kit>/index.html` directly).
- **`preview/*.html` use plain CSS**, not the published `.rcs-*` classes. They are brand cards (visual references), **not** demos of the React components — don't expect them to import from `src/`.

## Where to look next

- Architecture & layering → `docs/project/architecture.md`
- UI: tokens, themes, conventions, ui_kits → `docs/project/ui.md`
- Conventions (full set) → `docs/project/conventions.md`
- Build, release, dual-publish → `docs/project/build-and-run.md`
